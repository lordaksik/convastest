draw();
function draw() {
    const canvas = document.getElementById('tutorial');
    const ctx = canvas.getContext('2d');
    var fon = new Image();
    ctx.fillRect(100,50,600,40)
    ctx.fillRect(1200,50,600,40)
    //1900
}